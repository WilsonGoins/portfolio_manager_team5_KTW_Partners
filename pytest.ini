[pytest]
# server/ modules import each other flat (`from db_manager import DBManager`),
# so the package dir has to be on the path the same way it is under `flask run`.
pythonpath = server
testpaths = server/tests
